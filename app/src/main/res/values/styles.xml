<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="dayTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:textColorPrimary">@color/colorBlack</item>
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="menu_main_search">@drawable/ic_search_black_24dp</item>
        <item name="menu_main_delete">@drawable/ic_delete_forever_black_24dp</item>
    </style>

    <style name="dayTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="dayTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="dayTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
